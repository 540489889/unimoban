const baseUrl = ""; //域名
const miniappName = "分享标题"; //站点标题 ，一般用于分享时的标题


const domain = 'https://' + baseUrl + "/"; //接口地址（无需更改）
export default {
	baseUrl: baseUrl,
	domain: domain,
	miniappName: miniappName
}
